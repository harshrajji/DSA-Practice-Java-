class Solution {
    public int largestPerimeter(int[] nums) {
        Arrays.sort(nums);
        for(int i = nums.length-1; i >= 2; i--){
            int peri = 0;
            int a = nums[i];
            int b = nums[i-1];
            int c = nums[i-2];
            if((a +b) > c && (b +c) > a && (c+ a) > b){
                peri = a + b + c;
                return peri;
            }
        }
        return 0;
    }
}
